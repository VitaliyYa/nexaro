# generated by datamodel-codegen:
#   filename:  mqtt/climate_command.json
#   timestamp: 2026-09-01T10:33:43+00:00

from enum import StrEnum
from uuid import UUID

from pydantic import BaseModel, ConfigDict, Field, confloat


class HvacMode(StrEnum):
    """
    Desired operational mode
    """

    off = "off"
    cool = "cool"
    heat = "heat"
    fan_only = "fan_only"
    auto = "auto"
    dry = "dry"


class FanMode(StrEnum):
    """
    Desired fan speed
    """

    auto = "auto"
    low = "low"
    medium = "medium"
    high = "high"


class ClimateCommandPayload(BaseModel):
    """
    Command payload sent to control climate / AC setpoints and modes.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    target_temperature: confloat(ge=10.0, le=35.0) | None = Field(
        None, description="Target setpoint temperature in Celsius"
    )
    hvac_mode: HvacMode | None = Field(None, description="Desired operational mode")
    fan_mode: FanMode | None = Field(None, description="Desired fan speed")
    request_id: UUID = Field(..., description="Unique command idempotency tracking ID")
