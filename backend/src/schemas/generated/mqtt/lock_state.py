# generated by datamodel-codegen:
#   filename:  mqtt/lock_state.json
#   timestamp: 2026-09-01T10:33:43+00:00

from enum import StrEnum

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field, conint


class State(StrEnum):
    """
    Current lock mechanical state
    """

    locked = "locked"
    unlocked = "unlocked"
    jammed = "jammed"
    unknown = "unknown"


class LastTrigger(StrEnum):
    """
    Source or method of the last lock/unlock action
    """

    manual = "manual"
    passcode = "passcode"
    card = "card"
    fingerprint = "fingerprint"
    app = "app"
    auto_lock = "auto_lock"
    unknown = "unknown"


class LockStatePayload(BaseModel):
    """
    Reported physical state of a smart lock (e.g. TTLock).
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    state: State = Field(..., description="Current lock mechanical state")
    battery: conint(ge=0, le=100) = Field(..., description="Battery level percentage")
    last_trigger: LastTrigger | None = Field(None, description="Source or method of the last lock/unlock action")
    error: str | None = Field(None, description="Diagnostic error message or null if normal")
    timestamp: AwareDatetime = Field(..., description="ISO 8601 UTC timestamp of status update")
